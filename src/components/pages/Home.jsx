import React from 'react'
import Alert from 'react-bootstrap/Alert';

export const Home = () => {
  return (
    <Alert variant={'primary'}>Hello, you need to sign up/login to work with phonebook</Alert>
  )
}
